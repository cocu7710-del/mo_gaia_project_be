# 테스트 전용 설정 — 이 파일이 main의 application.properties를 대체한다 (datasource는 Testcontainers @ServiceConnection이 주입)
spring.application.name=mo_gaia_project_be
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false

# 리치 타임아웃 스위퍼 비활성 (컨테이너 종료 후 폴링 노이즈 방지)
gaia.leech-sweeper.enabled=false
