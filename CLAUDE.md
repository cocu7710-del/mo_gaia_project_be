# CLAUDE.md

이 파일은 Claude Code가 이 저장소에서 작업할 때 참고하는 가이드입니다.

## 프로젝트 개요

- **이름**: mo_gaia_project_be (`com.gaiaproject.mo_gaia_project_be`) — **백엔드(BE)** 프로젝트
  (폴더명은 아직 `mo_gaia_project_fe`이나 이후 `mo_gaia_project_be`로 변경 예정)
- **스택**: Java 17, Spring Boot 4.1.0 (Web MVC, Security, WebSocket, Data JPA, Flyway), Lombok, Gradle
- **상태**: 엔진(셋업·비딩·메인 액션 8종·함대·라운드 사이클·최종 점수) + 영속 계층(이벤트 소싱, 체크포인트 스냅샷, 언두 정책) + 인증(세션)·로비 + 리치 자동응답 설정·타이머 + WS 인증·구독 가드 + 채팅·리플레이 API까지 구현·테스트 완료. BE 다음 후보: CONSENT 언두 동의 UX, 관전 정책, 종료 시 final_score 저장. FE(React SPA)는 미착수

### 목표

보드게임 **가이아 프로젝트(Gaia Project) + 확장판 '잃어버린 함대(The Lost Fleet)'**를 웹 기반 게임으로 구현한다.

- **웹 + 모바일 웹** 모두 지원하되, **모바일 웹을 최우선 기준**으로 설계·개발한다 (mobile-first).
- 신규 개발이며, 기존 프로젝트의 리메이크가 아니다.

### 확정된 게임 구성

- **4인 전용** (2/3인 맵·중립 플레이어 룰 미지원)
- 용어는 `docs/glossary.md`의 용어 사전을 따른다 (예: TRANSDIM=차원 변형, BLACK_PLANET=검은행성=항법 5 보상, SCIENCE=지식 트랙, "잃어버린 행성"이라는 행성은 없음)
- 게임 데이터 확정값과 검수 현황은 `docs/data/*.md` 참조

### 확정된 아키텍처 결정

- **이 저장소 = BE 전용.** FE는 별도 프로젝트(React SPA 예정)로 만든다.
- **배포는 통합 가능**: FE 빌드 산출물을 Spring 정적 리소스로 포함해 단일 서버로 서빙하는 구조를 지향 (개발은 분리, 배포는 통합).
- **DB/인프라**: PostgreSQL + Docker. 현재는 개인 서버 배포, 이후 클라우드 이전 고려.
- **멀티플레이**: WebSocket(STOMP) 기반 실시간 온라인 멀티. AI 봇 상대는 이후 확장 가능하도록 설계만 고려.
- **인증**: 회원가입 + 로그인 방식.
- **서버 권위(server-authoritative) 구조**: 모든 룰 검증과 상태 변경은 서버가 최종 결정한다.
  - **결정 스택(decision stack) 기반 효과 해소 엔진**: 연구소 건설 → 기술 타일 선택 → 무료 광산 → 트랙 선택 → 검은 행성 … 처럼 액션이 실행 도중 새로운 결정을 연쇄적으로 낳으므로, "액션 전체를 한 번에 전송"이 아니라 **각 결정 단계가 독립적인 원자 트랜잭션**이다. 서버 엔진은 효과를 해소하다가 플레이어 선택이 필요하면 멈추고 "대기 중인 결정"을 스택에 push하며, 응답이 오면 검증 후 해소를 이어간다.
  - "지금 누구의 어떤 입력을 기다리는가"(pending decision 스택)는 **게임 상태의 일부로 DB에 저장**한다. 재접속/새로고침 시 그대로 복원되고, 스택 최상단과 맞지 않는 입력은 거부한다.
  - **파워 리치도 블로킹 결정으로 메인 스택에 올린다**: 이웃이 파워를 받는지 여부는 진행 플레이어의 이후 선택(예: 다음 광산을 리치 주지 않는 방향으로 지을지)에 필요한 **정보**이므로, 실물 게임과 동일하게 건설/업그레이드 커밋 직후 이웃들의 리치 결정을 시계방향 순서로 스택에 push하고, 전원 응답 후에 진행 플레이어의 다음 결정이 열린다.
    - 리치 제안량은 트리거 시점에 확정(트리거 건물 인접 건물들의 파워 값 합, 볼 상태에 따른 충전 상한 반영), 승점 비용 = 실제 충전량 − 1.
    - **속도 완화책**: ① 1파워 리치(승점 비용 0)는 기본 자동 수락, ② 플레이어별 "승점 비용 X 이하 자동 수락 / 이상 자동 거절" 개인 설정, ③ 방 설정 응답 타이머 초과 시 자동 거절. 타클론 브레인 스톤처럼 추가 선택이 있는 리치는 자동 응답에서 제외.
  - 단계별 UI 안내(유효 칸 하이라이트, 비용 계산)는 FE가 같은 룰 데이터로 사전 계산하되, 확정은 항상 서버가 한다. 각 결정 제출에 게임 상태 버전(시퀀스 번호)을 포함해 동시성 충돌을 감지한다.
  - **이벤트 소싱**: 게임 상태는 언제든 "스냅샷 + 액션 로그 재생"으로 재구성 가능해야 한다. 턴 시작마다 체크포인트를 남긴다. 이것이 턴 초기화(언두), 리플레이/관전, 버그 재현의 공통 기반이다.
  - **턴 초기화(언두)**: 턴 시작 마커 이후 로그를 무효화하고 상태를 재구성한다. 대기 중이던 결정 스택·리치 제안도 함께 사라지며, 전체 클라이언트에 새 버전으로 상태 리싱크를 브로드캐스트한다. 단계 단위 되돌리기도 같은 방식.
    - **언두 정책**: 상대가 아직 아무 결정도 응답하지 않았으면(미응답 리치 제안만 떠 있는 상태 포함) 자유 언두. 상대가 이미 응답했으면(자동 응답 포함) 영향받은 플레이어의 동의 필요 — "리치 반응 보고 무르기" 악용 차단. 방 생성 옵션으로 친선(자유)/표준(동의제)/경쟁(언두 금지) 선택.
  - 종족 특수 능력(타클론 브레인 스톤, 하이브 연방 선언 등)도 별도 분기 코드가 아니라 이 프레임 안의 "결정 타입"으로 통일해 처리한다.

### 기존 v1의 알려진 버그 (재설계 시 주의)

v1은 대부분의 기능이 정상 동작하는 수준까지 구현됐지만, 아래 영역에 버그가 있었다.
새 설계에서 이 영역들은 특히 꼼꼼히 규칙을 정의하고 테스트를 갖춘다:

- **종족(캐릭터) 능력 버그**: 타클론(Taklons)의 브레인 스톤, 하이브(Ivits)의 연방 선언 등 특수 능력 처리
- **고급 기술 타일 획득** 처리
- **연방 타일 획득 능력** 처리

### 기획 문서

`docs/` 참조 — 용어 사전(glossary), 게임 데이터(data/*), 결정 연쇄 표(rules/decision-flows), 엣지 케이스 확정 룰(rules/edge-cases), 도메인 모델(design/domain-model). 인덱스는 `docs/README.md`.

### 기존 참고 프로젝트 (v1, 약 80% 완성)

| 구분 | 경로 | 스택 |
|------|------|------|
| BE | `C:\Users\cocu7\OneDrive\Desktop\PJ\gaia-project` | Spring Boot, MySQL, WebSocket(STOMP), Docker |
| FE | `C:\Users\cocu7\OneDrive\Desktop\PJ\gaia_project_fe` | React 19, TypeScript, Vite, Tailwind 4, Zustand, STOMP/SockJS |

**참고 원칙 (중요):**
- 기존 프로젝트는 여러 버그가 있으므로 **코드·테이블 구조·작동 방식을 그대로 가져오지 않는다.**
- **게임 데이터만 참조**한다: 각 종족(캐릭터) 능력, 맵 생성 로직/구조, 게임 구성 요소 데이터 등.
- 테이블 구조, 도메인 모델, 작동 방식은 **새로 기획·설계**한다.

## 빌드 & 실행 명령어

```bash
./gradlew build          # 빌드
./gradlew bootRun        # 애플리케이션 실행
./gradlew test           # 전체 테스트 실행
./gradlew test --tests "com.gaiaproject.mo_gaia_project_fe.클래스명"  # 단일 테스트
```

Windows에서는 `gradlew.bat`을 사용한다.

## 코드 구조

- 메인 진입점: `src/main/java/com/gaiaproject/mo_gaia_project_fe/MoGaiaProjectFeApplication.java`
- 설정: `src/main/resources/application.properties`
- 테스트: `src/test/java/com/gaiaproject/mo_gaia_project_fe/`

## 코딩 행동 지침 (Karpathy Guidelines)

LLM 코딩에서 흔한 실수를 줄이기 위한 행동 원칙. 사소한 작업에는 유연하게 적용하되, 그 외에는 신중함을 속도보다 우선한다.
(출처: [karpathy-guidelines](https://github.com/multica-ai/andrej-karpathy-skills/tree/main/skills/karpathy-guidelines), MIT)

### 1. 코딩 전에 생각하기 (Think Before Coding)

**가정하지 말 것. 혼란을 숨기지 말 것. 트레이드오프를 드러낼 것.**

구현 전에:
- 가정을 명시적으로 밝힌다. 불확실하면 질문한다.
- 해석이 여러 가지면 조용히 하나를 고르지 말고 선택지를 제시한다.
- 더 단순한 방법이 있으면 말한다. 필요하면 반론을 제기한다.
- 불명확한 부분이 있으면 멈추고, 무엇이 혼란스러운지 명명하고, 질문한다.

### 2. 단순함 우선 (Simplicity First)

**문제를 해결하는 최소한의 코드. 추측성 코드 금지.**

- 요청받지 않은 기능을 추가하지 않는다.
- 한 번만 쓰이는 코드에 추상화를 만들지 않는다.
- 요청되지 않은 "유연성"이나 "설정 가능성"을 넣지 않는다.
- 일어날 수 없는 시나리오에 대한 에러 처리를 하지 않는다.
- 200줄로 짰는데 50줄로 가능하다면 다시 짠다.

자문할 것: "시니어 엔지니어가 이걸 보고 과하다고 할까?" 그렇다면 단순화한다.

### 3. 외과적 수정 (Surgical Changes)

**꼭 필요한 부분만 건드린다. 내가 만든 흔적만 치운다.**

기존 코드를 수정할 때:
- 주변 코드·주석·포맷을 "개선"하지 않는다.
- 고장 나지 않은 것을 리팩토링하지 않는다.
- 내 취향과 달라도 기존 스타일을 따른다.
- 무관한 죽은 코드를 발견하면 삭제하지 말고 언급만 한다.

내 변경으로 고아가 생겼을 때:
- 내 변경 때문에 안 쓰이게 된 import/변수/함수는 제거한다.
- 원래 있던 죽은 코드는 요청 없이는 제거하지 않는다.

검증 기준: 변경된 모든 줄이 사용자의 요청으로 직접 추적 가능해야 한다.

### 4. 목표 주도 실행 (Goal-Driven Execution)

**성공 기준을 정의하고, 검증될 때까지 반복한다.**

작업을 검증 가능한 목표로 변환:
- "검증 추가해줘" → "잘못된 입력에 대한 테스트를 작성하고 통과시킨다"
- "버그 고쳐줘" → "버그를 재현하는 테스트를 작성하고 통과시킨다"
- "X 리팩토링해줘" → "리팩토링 전후로 테스트가 통과함을 확인한다"

여러 단계 작업은 간단한 계획을 먼저 제시:

```
1. [단계] → 검증: [확인 방법]
2. [단계] → 검증: [확인 방법]
3. [단계] → 검증: [확인 방법]
```

성공 기준이 명확하면 독립적으로 반복 작업이 가능하다. "되게 해줘" 같은 약한 기준은 계속 확인이 필요해진다.
